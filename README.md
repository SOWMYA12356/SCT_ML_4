This project recognizes hand gestures using a machine learning model.

Dataset: Leap Gesture Recognition Dataset

Tools used:
Python
OpenCV
NumPy
Scikit-learn
The model is trained using hand gesture images and predicts the gesture class.
The trained model is saved as gesture_model.pkl.

